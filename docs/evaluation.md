# Evaluating classifier quality

Evaluation compares predictions with human-labelled examples. In addition to aggregate accuracy, inspect:

- per-vibe precision and recall
- confusion patterns
- confidence calibration
- low-margin/ambiguous predictions

A small but carefully labelled dataset is more useful for regression tracking than anecdotal examples because the same corpus can be evaluated after every scoring change.
